package modificadores;

// uma classe apenas pode ser public ou default
// os método e atributos podem ser protect, private, public ou default
public class PessoaProtected {

    // Protected é visivel apenas dentro do pacote e através de herança
    protected String nome;
    protected int idade;
}
